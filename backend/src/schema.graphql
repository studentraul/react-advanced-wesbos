# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String
    description: String
    image: String
    largeImage: String
    price: Float
  ): Item!
}

type Query {
  items: [Item]!
}
